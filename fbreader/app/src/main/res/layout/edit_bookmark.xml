<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
	xmlns:android="http://schemas.android.com/apk/res/android"
	xmlns:app="http://schemas.android.com/apk/res-auto"
	android:id="@+id/edit_bookmark"
	android:layout_width="500dp"
	android:layout_height="350dp"
	android:orientation="vertical"
>
	<LinearLayout
		android:layout_height="wrap_content"
		android:layout_width="match_parent"
		android:theme="@style/FBReaderMD.ActionBar"
		android:orientation="horizontal"
		android:background="@drawable/primary_color"
	>
		<include
			layout="@layout/md_toolbar"
			android:layout_height="match_parent"
			android:layout_width="wrap_content"
		/>
		<android.support.design.widget.TabLayout
			android:id="@+id/edit_bookmark_tab_layout"
			android:layout_height="match_parent"
			android:layout_width="wrap_content"
			android:background="@drawable/primary_color"
			background="@drawable/primary_color"
		/>
	</LinearLayout>
	<android.support.v4.view.ViewPager
		android:id="@+id/edit_bookmark_view_pager"
		android:layout_width="match_parent"
		android:layout_height="match_parent"
		app:layout_behavior="@string/appbar_scrolling_view_behavior"
	/>
</LinearLayout>
